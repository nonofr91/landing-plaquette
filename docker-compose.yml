services:
  landing-plaquette:
    build: .
    environment:
      - MAUTIC_URL=${MAUTIC_URL}
      - MAUTIC_USER=${MAUTIC_USER}
      - MAUTIC_PASSWORD=${MAUTIC_PASSWORD}
      - PORT=3000
    ports:
      - "3000"
